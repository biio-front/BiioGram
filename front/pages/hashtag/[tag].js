import React from 'react';
import AppLayout from '../../components/layout/AppLayout';

const hashtag = () => {
  return (
    <AppLayout>
      <p>이 기능은 준비 중입니다.</p>
    </AppLayout>
  );
};

export default hashtag;
